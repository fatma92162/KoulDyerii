<li class="nav-item">
    <a class="nav-link" href="{{ path('app_admin_notifications') }}">
        🔔 Notifications 
        {% set unreadCount = entityManager.getRepository(AdminNotification::class).countBy(['isRead' => false]) %}
        {% if unreadCount > 0 %}
            <span class="badge bg-danger">{{ unreadCount }}</span>
        {% endif %}
    </a>
</li>